## Cheat Sheet
`base64.b64encode()`
> import base64**

used to encode to base64

`bytes.fromhex()` :

decodes the hex to bytes

`long_to_bytes(n)`:

>from Crypto.Util.number import *

1. Converts integer → hexadecimal
2. Splits hex into byte pairs
3. Converts each byte to ASCII
4. Returns original message

`long_to_bytes()` :

are inverse functions.

`xor()` :

>from pwn import xor

used for xor'ing strings

